import React from 'react';
import PropagateLoader from 'react-spinners/PropagateLoader';

const LoadingSpinner = () => {
  return (
    <PropagateLoader color="#99D98C" size={30} />
  );
};

export default LoadingSpinner;
